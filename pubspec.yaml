name: daily_plan_app
description: 日常计划管理应用
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI组件
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  
  # 状态管理
  provider: ^6.1.1
  
  # 数据库
  supabase_flutter: ^2.3.4
  
  # 工具库
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  
  # 图标
  flutter_svg: ^2.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
