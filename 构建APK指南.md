# 📱 Flutter 日常计划应用 - 构建APK指南

## 🎉 好消息！

这是一个 **Flutter 应用**，可以直接生成APK安装包！

## 📋 准备工作

### 1. 安装 Flutter SDK

**Windows 系统：**

1. 下载 Flutter SDK
   - 访问：https://docs.flutter.dev/get-started/install/windows
   - 下载最新稳定版（约 1.5GB）

2. 解压到目录（如 `C:\flutter`）

3. 添加环境变量
   - 打开"系统属性" → "环境变量"
   - 在"Path"中添加：`C:\flutter\bin`

4. 验证安装
   ```bash
   flutter --version
   ```

### 2. 安装 Android Studio（可选，推荐）

如果想在电脑上调试：
- 下载：https://developer.android.com/studio
- 安装 Android SDK

## 🚀 快速构建APK（3步完成）

### 方法1：使用命令行（最简单）

打开 PowerShell 或 CMD，执行以下命令：

```bash
# 1. 进入项目目录
cd "D:\我的文档\日常计划Flutter应用"

# 2. 获取依赖
flutter pub get

# 3. 构建APK
flutter build apk --release
```

**完成！** APK文件位置：
```
D:\我的文档\日常计划Flutter应用\build\app\outputs\flutter-apk\app-release.apk
```

### 方法2：使用一键构建脚本

我已经为您创建了构建脚本，双击运行即可：

**Windows:**
```
构建APK.bat
```

## 📦 APK 文件说明

构建完成后，您会得到：
- **文件名**：`app-release.apk`
- **大小**：约 20-30 MB
- **位置**：`build\app\outputs\flutter-apk\`

## 📲 安装到手机

### 方法1：USB 连接

1. 用USB线连接手机和电脑
2. 手机开启"USB调试"
3. 执行命令：
   ```bash
   flutter install
   ```

### 方法2：文件传输

1. 将 `app-release.apk` 复制到手机
2. 在手机上找到APK文件
3. 点击安装（需要允许"未知来源"）

### 方法3：扫码安装

使用文件传输工具（如微信、QQ）发送APK到手机。

## ⚙️ 系统要求

### 开发电脑：
- Windows 10/11
- 磁盘空间：至少 5GB
- 内存：至少 4GB（推荐 8GB）

### 安卓手机：
- Android 5.0 (API 21) 或更高版本
- 支持几乎所有安卓手机

## 🎯 构建选项

### 构建不同版本

```bash
# 构建 Release 版本（推荐，文件小）
flutter build apk --release

# 构建 Debug 版本（用于调试）
flutter build apk --debug

# 构建分架构APK（文件更小）
flutter build apk --split-per-abi
```

### 分架构构建说明

如果使用 `--split-per-abi`，会生成3个APK：
- `app-armeabi-v7a-release.apk` - 32位ARM（老手机）
- `app-arm64-v8a-release.apk` - 64位ARM（新手机，推荐）
- `app-x86_64-release.apk` - x86架构（模拟器）

**大多数手机选择：`app-arm64-v8a-release.apk`**

## 🔧 常见问题

### Q1: 提示"flutter不是内部或外部命令"
**A:** Flutter 未安装或环境变量未配置。请重新安装 Flutter 并配置环境变量。

### Q2: 构建失败，提示网络错误
**A:** 可能是网络问题。尝试：
```bash
flutter pub get --verbose
```

### Q3: 手机无法安装APK
**A:** 需要在手机设置中允许"安装未知应用"：
- 设置 → 安全 → 未知来源 → 允许

### Q4: 构建时间很长
**A:** 首次构建需要下载依赖，约需 10-20 分钟。后续构建会快很多（1-3分钟）。

### Q5: 提示 Android SDK 未找到
**A:** 两个解决方案：
1. 安装 Android Studio（推荐）
2. 单独安装 Android SDK 命令行工具

## 📝 构建日志

如果构建失败，查看详细日志：
```bash
flutter build apk --release --verbose
```

## 🎨 自定义应用

### 修改应用名称
编辑文件：`android\app\src\main\AndroidManifest.xml`
```xml
<application
    android:label="你的应用名称"
    ...>
```

### 修改应用图标
替换文件：
- `android\app\src\main\res\mipmap-hdpi\ic_launcher.png`
- `android\app\src\main\res\mipmap-mdpi\ic_launcher.png`
- `android\app\src\main\res\mipmap-xhdpi\ic_launcher.png`
- `android\app\src\main\res\mipmap-xxhdpi\ic_launcher.png`
- `android\app\src\main\res\mipmap-xxxhdpi\ic_launcher.png`

### 修改包名
编辑文件：`android\app\build.gradle`
```gradle
defaultConfig {
    applicationId "com.yourcompany.yourapp"
    ...
}
```

## 🚀 快速开始（完整流程）

```bash
# 1. 安装 Flutter（首次）
# 下载并解压 Flutter SDK，配置环境变量

# 2. 验证安装
flutter doctor

# 3. 进入项目目录
cd "D:\我的文档\日常计划Flutter应用"

# 4. 获取依赖
flutter pub get

# 5. 构建APK
flutter build apk --release

# 6. 找到APK文件
# 位置：build\app\outputs\flutter-apk\app-release.apk

# 7. 传输到手机并安装
```

## 📞 需要帮助？

如果遇到问题：

1. **查看 Flutter 文档**
   - https://docs.flutter.dev/

2. **运行诊断**
   ```bash
   flutter doctor -v
   ```

3. **清理并重新构建**
   ```bash
   flutter clean
   flutter pub get
   flutter build apk --release
   ```

## ✅ 成功标志

构建成功后，您会看到类似输出：
```
✓ Built build\app\outputs\flutter-apk\app-release.apk (XX.XMB).
```

**恭喜！您的APK已经生成，可以安装到手机了！** 🎉

---

## 📱 应用功能

安装后，您可以：
- ✅ 记录每日运动（跑步、游泳等）
- ✅ 记录学习情况（网络、系统集成、英语）
- ✅ 查看今日统计
- ✅ 查看历史数据
- ✅ 数据云端同步（Supabase）

**开始您的每日进步之旅吧！** 💪
